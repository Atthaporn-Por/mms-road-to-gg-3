matrix:
  include:
    - language: python
      python:
        - 3.6
        - nightly
      install:
        - pip install PyMySQL
      script:
        - python Test/test_test.py

    - language: node_js
      node_js:
        - "7"
      install:
        - npm install
        - npm install -g gulp
        - npm init
      script: 
        - gulp
